<%- include('head') %>
<div id="content" class="d-flex flex-column">
<header class="bg-dark text-white mb-4">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center pt-2">
            <a href="/" class="muted-anchor">
                <h1 class="h2 text-nowrap pt-2 pb-3 m-0 text-white"> Forॐ </h1>
            </a>
            <form method="post" action="/search" class="input-group d-none d-md-flex col-5">
                <input type="text" name="search" required class="form-control bg-white" placeholder="Search a user or a post">
                <div class="input-group-append">
                    <button type="submit" class="input-group-text">
                        <i class="now-ui-icons ui-1_zoom-bold"></i>
                    </button>
                </div>
            </form>
            <% if (logged) { %>
                <a href="/logout" class="btn btn-danger btn-icon btn-round">
                    <i class="now-ui-icons media-1_button-power"></i>
                </a>
            <% } %>
        </div>
    </div>
</header>
<main class="container flex-grow-1 d-md-flex justify-content-around">
