<%- include('../partials/header', { options: { user: null }, title: 'Error' }); %>

<div class="align-self-center shadow rounded">
    <h1 class="text-red">Error</h1>
    <p>
        <%- message %>
        <% if(!message.includes('href')){ %>
            <a href='/'>Back to home.</a>
        <% } %>
    </p>
</div>

<%- include('../partials/footer'); %>