<%- include('../partials/header', { options: { user }, title: 'Topic' }); %>

<div class="flex-grow-1">
    <h1> Topic </h1>

    <%- include('../partials/post-breadcrumb', { post }) %>

    <div class="d-flex">
        <%- include('../partials/post', { user, post }) %>
    </div>

    <h2> Comments </h2>

    <%- include('../partials/post-form', { action: 'comment', user, post }) %>
    <%- include('../partials/post-list', { pagination: post.getChildrenPagination(pageIndex), user }) %>
</div>

<%- include('../partials/friends', { user, target: user }); %>
<%- include('../partials/footer') %>